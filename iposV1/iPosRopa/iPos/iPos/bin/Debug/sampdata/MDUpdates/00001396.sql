CREATE OR ALTER VIEW DOCTOIEPSVIEW(
    DOCTOID,
    TASAIEPS,
    SUMAIEPS)
AS
SELECT
DOCTOID AS DOCTOID,
COALESCE(TASAIEPS,0) TASAIEPS,
SUM(COALESCE(IEPS,0)) SUMAIEPS
FROM
movto
group by doctoid, COALESCE(TASAIEPS,0)
;