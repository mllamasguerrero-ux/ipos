EXECUTE block as
 BEGIN
 if (not exists(
 select 1 from RDB$RELATION_FIELDS rf
 where rf.RDB$RELATION_NAME = 'PARAMETRO' and rf.RDB$FIELD_NAME = 'MOSTRARINVINFOADICPROD'))
 then
 execute statement 'ALTER TABLE PARAMETRO ADD MOSTRARINVINFOADICPROD D_BOOLCN';
 END